# actioning

This directory is a required project-root marker for the pinned structural
governance orchestrator. Formal Action business artifacts may be written below
`actioning/pipelines/`; Demo tests must continue to redirect every generated
artifact to an isolated directory below `temp/`.

