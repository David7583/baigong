# v0018 核心流水线发布候选规则

本文件同时作为流水线项目根标识，必须随源代码包保留。

- 本包仅包含 v0018 核心流水线及其依赖，入口与安装方法见 README.md。
- 未经明确授权不得推送、发布、调用收费服务、删除原始数据或改写已有业务数据库。
- 验收仅使用人工样本，使用独立 temp 与唯一 run-id，真实本地模型必须显式指定；不得把 mock 结果当作真实模型结果。
- 不提交密钥、模型权重、个人资料、虚拟环境或运行数据库；维护来源、哈希、测试证据和回退记录。
- 同任务的迭代继续使用同一工作目录和 Git；本解压副本仅用于验收，不代表新任务。
- 在宿主项目中使用时，遵守宿主更具体的开发、应用权限和 AI 总控接入要求。本包不授权绕过这些规则。
